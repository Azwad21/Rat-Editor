import React from 'react'

const Tree = (props) => {
    console.log(props.paths)

    return (
        <div className="rat-editor-explorer-tree">
            
        </div>
    )
}

export default Tree
